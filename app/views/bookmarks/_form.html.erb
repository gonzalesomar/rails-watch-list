<div class="container">
  <%= simple_form_for [list, bookmark] do |f| %>
    <%= f.input :comment %>
    <%= f.submit %>
  <% end %>

  <hr>

  <p><%= link_to "Back to list", list_path(list), class: "btn btn-info"%> </p>
</div>
